# ft-dash 词汇表

只放术语，不放实现细节。代码里同一概念必须用同一个词；这里的英文名就是代码里的标识符。

## 数据

| 术语 | 代码 | 含义 |
| --- | --- | --- |
| 交易对 | `pair` | 交易标的，形如 `BTC/USDT`。**不要**写成「配对」。 |
| 交易（一笔） | `trade` | 机器人记录的一次买卖，包含开仓与平仓两段。持仓中的也是一笔 trade。 |
| 当前持仓 | `openTrades` | `state == 'open'` 的 trade，界面上是「当前持仓」。**不要**新造 `openPositions`。 |
| 已平仓 | `closedTrades` | `state == 'closed'` 的 trade，「最近平仓」「已平仓」都指它。 |
| 挂单 / 订单 | `order` | 一笔 trade 内部的交易所订单。 |
| 方向 | `side` | `long` / `short`；订单的方向用 `orderSide`。 |

## 金额

| 术语 | 代码 | 含义 |
| --- | --- | --- |
| 账户净值 | `balance.total` | 钱包总值（含持仓估值）。 |
| 可用余额 | `availableBalance` | 可再投入的余额。 |
| 持仓市值 | `positionValue` | 当前持仓的估值合计。 |
| 总盈亏 | `profit_all_coin` | **账户口径**：所有交易的累计盈亏。 |
| 已平仓盈亏 | `profit_closed_coin` | **账户口径**：只算已平仓的那些。 |
| 本对盈亏 | `pairProfit` | **交易对口径**：某一个交易对自身的累计盈亏（表格列）。 |

> 账户口径与交易对口径是两个不同的东西，标签不能混用——「本对盈亏」就是为此存在的。

## 绩效

| 术语 | 代码 | 含义 |
| --- | --- | --- |
| 胜率 | `winRate` | 盈利笔数 ÷ 已决出胜负的笔数。交易对表格里的同名列是该交易对自己的胜率。 |
| 盈亏比 | `profit_factor` | 总盈利 ÷ 总亏损；没有亏损单时无法计算，显示 `—`。 |
| 期望值 | `expectancy` | 每笔交易的期望收益。 |
| 夏普 / 索提诺 | `sharpe` / `sortino` | 收益 ÷ 波动率 / 收益 ÷ 下行波动。 |
| 最大回撤 | `max_drawdown` | 历史最大回撤（比例，界面转成百分比）。 |
| 哨兵值 | `-100` | Freqtrade 在分母为 0（无法计算）时返回的标记，不是真实读数。 |

## 运行时

| 术语 | 代码 | 含义 |
| --- | --- | --- |
| 心跳 | `health.last_process` | 机器人进程每一轮的时间戳；界面显示它的**延迟**（已过去多久）。 |
| 心跳静默 | `HEARTBEAT_STALE_MS` | 心跳晚于这个时长就认为机器人卡住。 |
| 轮询 | `POLL_INTERVAL_SECONDS` | 前端拉取核心数据的基础节奏；较重的数据分批拉得更慢。 |
| 实时推送 | `stream` | Freqtrade 的 WebSocket 事件流；`wsDisabled` 等沿用 Freqtrade 的 `ws_token` 叫法。 |
| 实时事件 | `events` | 推送里的事件条目（入场、成交、撤单、保护机制触发…）。 |
| 机器人控制 | `allowControls` / `controlsAcknowledged` | 写操作总开关与风险确认，两者同时为真才算开启。 |
| 交易锁 | `locks` | Freqtrade 的 pair lock。 |

## 界面

| 术语 | 含义 |
| --- | --- |
| 磁贴 | 顶部那排指标卡（`.metric`），一个数加一行小字。 |
| 面板 | 一块带标题的内容区（`.panel`）。 |
| 抽屉 | 点开单笔交易后从右侧（手机从下往上）出现的详情面板。 |
| 提示条 | 底部弹出的 toast，带倒计时进度条。 |
